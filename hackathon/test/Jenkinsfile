pipeline { 
    environment { 
        registry = "vgb5555/hackathon" 
        registryCredential = 'dockerhub_id' 
        dockerImage = '' 
    }
    agent any 
    stages { 
        stage('Cloning our Git') { 
            steps { 
                git 'https://github.com/vbhutmar/DevSecOps.git' 
            }
        } 
        stage('Building our image') { 
            steps { 
                script { 
                    dockerImage = docker.build ("vgb5555/test")
                }
            } 
        }
        stage('Push image to Docker hub') { 
            steps { 
                script { 
                    docker.withRegistry( '', registryCredential ) { 
                        dockerImage.push() 
                    }
                } 
            }
        } 
        stage('Cleaning up') { 
            steps { 
                sh "docker rmi ("vgb5555/test")" 
            }
        } 
    }
}
